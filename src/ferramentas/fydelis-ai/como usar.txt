# 1. Instalar dependências
cpanm Moo Types::Standard Try::Tiny HTTP::Tiny JSON YAML::XS Term::ANSIColor

# 2. Verificar se Ollama está rodando
./fydelis-ai -a ping

# 3. Modo interativo
./fydelis-ai -a interactive

# 4. Pergunta direta
./fydelis-ai -q "Como fazer bypass de autenticação em formulários web?"

# 5. Com alvo específico
./fydelis-ai -t 192.168.1.100 -q "Quais serviços rodam nas portas comuns?"

# 6. Sugerir comando
./fydelis-ai -t scanme.org -a suggest -q "scaneie o alvo para encontrar painéis admin"

# 7. Executar e analisar
./fydelis-ai -a exec -c "nmap -sV -T4 scanme.org"

# 8. Analisar output existente
./fydelis-ai -a analyze -f output/nmap_result.txt --tool-type nmap

# 9. Usar modelo diferente
./fydelis-ai -m llama3:8b -q "Explique Server-Side Template Injection"

# 10. Ver estatísticas do cache
./fydelis-ai -a cache-stats

analisar novos parsers

# Analisar output do WPScan
./fydelis-ai -a analyze -f output/wpscan.txt --tool-type wpscan

# Analisar output do OpenVAS (XML, CSV, HTML ou texto)
./fydelis-ai -a analyze -f output/openvas-report.xml --tool-type openvas

# Analisar output do Metasploit
./fydelis-ai -a analyze -f output/msfconsole.log --tool-type metasploit

# Auto-detect (tenta identificar automaticamente)
./fydelis-ai -a analyze -f output/qualquer-coisa.txt